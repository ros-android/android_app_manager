<?xml version="1.0" encoding="UTF-8"?>
<project name="AppManAndroid" default="help">

    <!-- This set of lines is a huge hack that can be gotten rid of once we 
	 remove the library directory, see pr2apps #3 -->
    <property file="../project.properties" prefix="file" />
    <property name="android.library.reference.1" value="../${file.android.library.reference.1}" />
    <property name="target" value="${file.target}" />
    <property name="sdk.dir" value="${file.sdk.dir}" />
    <property name="android.library" value="${file.android.library}" />
    <!-- Replace this with this -->
    <!-- <property file="../project.properties" /> -->


    <property file="../ros.properties" />

    <property file="ant.properties" />

    <!--loadproperties srcFile="project.properties" /-->

    <fail
            message="sdk.dir is missing. Make sure to generate local.properties using 'android update project' or to inject it through an env var"
            unless="sdk.dir"
    />

    <target name="-pre-compile">
      <mkdir dir="libs"/>
      <copy todir="libs" flatten="true">
         <filelist dir="/" files="${ros.compile.jarfileset}"/>
      </copy>
    </target>

    <!--target name="-post-compile">
      <copy file="bin/classes.jar" tofile="bin/appmanandroid.jar"/>
    </target-->

    <!-- version-tag: 1 -->
    <import file="${sdk.dir}/tools/ant/build.xml" />

</project>
